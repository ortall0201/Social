# Buffer delivery (repo pointer)

**Binaries do not live here.** Put reels, stills, and caption cards for local Buffer prep under:

**`buffer-local/`** (gitignored at repo root).

This folder stays in Git so paths like `buffer-delivery/` remain a stable **documentation / convention** anchor. Copy or export finished assets to **`https://iris-media.onsight-analytics.com/approved/<filename>`** (or another HTTPS you control) before calling `scripts/buffer/buffer-queue-*.ps1`.
