# Local workflow archives
old_workflows/

# Local video storage
local_videos/

# n8n workflows (keep local only)
workflows/

# Documentation and reference files
files/

# n8n brain files (keep local only)
n8n-brain/

# Brain files (keep local only)
brain/

# Devi content guides (keep local only)
devi-content/README.md
devi-content/DEVI-VIRAL-CAPTION-GUIDE.md

# MCP config (contains secrets)
.mcp.json

# Agent-related files (local only)
.agents/
.claude/agents/
.claude/skills/ortal-the-great-linkedin/
.claude/skills/linkedin-engagement-psychology/
.claude/skills/linkedin-leads-outreach-safety/
.claude/skills/agent-memory-wiki-pattern/
.claude/skills/linkedin-buffer-operations/
.claude/skills/linkedin-dm-boundaries/
*.agent.json
adb_mcp_server.py
.cursor/rules/
.cursor/skills/linkedin-engagement-psychology/
.cursor/skills/linkedin-leads-outreach-safety/
.cursor/skills/ortal-the-great-linkedin/
.cursor/skills/agent-memory-wiki-pattern/
.cursor/skills/linkedin-buffer-operations/
.cursor/skills/linkedin-dm-boundaries/

# Skills (local only)
skills/

# Temp ADB frames (local only)
tmp_frames/

# Python scripts (local only)
*.py

# Iris control panel + agent registry (local only)
IRIS.md
agents.md

# Local Buffer working copies (mp4/jpg/png). See tracked `buffer-delivery/README.md`.
# Promote to iris-media / CDN / approved HTTPS before scheduling in Buffer.
buffer-local/

# Generated / non-repo Buffer assets (keep local; do not commit)
artifacts/

# Misc local files
nul
devi_vton_colab.ipynb
devi_vton_colab_api.ipynb

# Devi video images
devi-videos/images/
devi-identity/images/*.jpeg

# Devi video outputs (local only)
devi-videos/*.mp4
devi-videos/videos/
motion videos/
tmp_screen.png

# Contenty pipeline runs (local only)
contenty/

# Claude local config and memory
.claude/agent-memory-local/
.claude/settings.local.json

# Project instructions (local only, contains system context)
CLAUDE.md

# Production runtime state
.state/
services/iris-control-plane/.state/

# Local deployment secrets and env files
deploy/
deploy/staging/secrets/approval_shared_secret.txt
deploy/production/env/*.env
deploy/production/env/*.local
local-secrets/*

# Work Manager bridge (local CLI chat IDs + last clipboard extracts)
scripts/wm-local.ps1
scripts/.wm-last-iris-prompt.txt
scripts/.wm-last-codex-prompt.txt
scripts/.wm-last-event-scout-prompt.txt

# Operator dashboard local DB
services/operator-dashboard/data/

# Private local runtime / agent / worker system (never publish)
docs/
scripts/
!devi-remotion/scripts/
!devi-remotion/scripts/**
services/

# LinkedIn agent work is local-only and must never be published
docs/agents/linkedin-lead-agent.md
docs/agents/ortal-linkedin-persona.md
brain/roadmaps/linkedin/
docs/social-plan.md
